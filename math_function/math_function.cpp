#include <iostream>
#include <cmath>

int main() {

  double x = 3.14;
  double y  = 4;
  double z;

  // z = std::max(x, y);
  // z = std::min(x, y);

  // 아래는 cmath 라이브러리 추가해야 함
  // z = pow(2, 3);
  // z = sqrt(9);
  // z = abs(-4);
  // z = round(x);
  // z = ceil(x);
  // z = floor(x);

  std::cout << z;

  return 0;
}